import React from 'react'
import useOnline from '../utills/useOnline'
import useCounter from '../utills/useCounter'

const A = () => {
  //   const online = useOnline()
  //    const{count,incirment,decirment} =useCounter()
  return (
    <>
      {/* <h2>
        user -
        {online ? (
          <img src="/images.png" alt="" width={80} />
        ) : (
          <img src="../../public/offline.png" width={80} />
        )}
      </h2> */}

      {/* 
          <p>{count}</p>
          <button onClick={incirment}>INCR</button>
          <button onClick={decirment}>DEC</button> */}
    </>
  )
}

export default A
